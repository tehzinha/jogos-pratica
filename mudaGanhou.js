

document.addEventListener('keydown', (event) => {
  if (event.key === 'Enter') {
    document.body.style.backgroundColor = 'blue';
  }
});

//document.addEventListener('keydown', (event) => {
 // if (event.key === 'Enter') {
//    document.body.style.backgroundImage = "url('mesa.jpg')";  //Substitua com o caminho da nova imagem
//  }
//});
